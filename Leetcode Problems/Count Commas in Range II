
/**
 * @param {number} n
 * @return {number}
 */
var countCommas = function(n) {
    let bigN = BigInt(n);
    let res = 0n;
    let p = 1000n;

    while (p <= bigN) {
        res += bigN - p + 1n;
        p *= 1000n;
    }

    return Number(res);
};
